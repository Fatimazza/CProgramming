#include <stdio.h>

int main()
{
    // Declaring variables
    int age = 17;
    float gpa = 3.75;
    char grade = 'A';

    // Printing variable values
    printf("Age: %d\n", age);
    printf("GPA: %f\n", gpa); // Format to 2 decimal places
    printf("Grade: %c\n", grade);

    return 0;
}